{
  "name": "snapshare",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd client && npm run dev\"",
    "install-all": "cd backend && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
